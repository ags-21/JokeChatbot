# JokeChatbot
Retrieval-augmented joke chatbot built with Flask, Docker, and deployed to Google Cloud Run. Uses a curated 100-joke database as LLM context via OpenRouter (Claude 3 Haiku) — the model retrieves, never generates. Full pipeline from data fetching to containerised cloud deployment.
